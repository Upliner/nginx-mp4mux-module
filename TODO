m3u8: move static parts to separate nginx buffers
m3u8: use relative paths
use log10-like functions instead of INT_MAX when allocating memory
move Content-Disposition generation code to single place?
use SHA1 in ETag
hls: implement stuffing small packets
hls: check and debug packet counting
PPS/SPS bound checks
use constants instead of magic numbers
mp4mux_preread() is hack, get rid of it
use separate rdbufs for unaligned file tails, read them synchronously in mp4mux_read_direct()
offs_restart is hack, get rid of it
Implement aio
